
/// A struct defining the `GraphQLType` attribute.
public struct GraphQLTypeAttribute {
    /// Holds the `GraphQLType` that can be modified by the `graphQLType()`modifier.
    var graphQLType: GraphQLType
}
