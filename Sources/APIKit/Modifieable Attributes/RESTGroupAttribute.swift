
/// A struct defining the REST Group attribute.
public struct RESTGroupAttribute {
    /// Holds the REST group `PathComponent`s that can be modified by the `RESTGroup()`modifier.
    var pathComponents: [PathComponent]?
}
